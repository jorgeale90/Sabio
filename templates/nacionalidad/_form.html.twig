{{ form_start(form) }}
<div class="card-body">
    <div class="form-group row">
        <label class="col-sm-3 col-form-label">País <span class="required">*</span> :</label>
        <div class="col-sm-7">
            {{ form_widget(form.nombre, { 'attr': { 'class': 'form-control', 'required': 'required', 'placeholder': 'Nombre del País' } }) }}
        </div>
        <div class="col-2">
            {% if(form_errors(form.nombre)) %}
                <div class="buttons">
                    <a href="#" class="btn btn-icon btn-warning" data-container="body" data-toggle="popover" data-placement="top" data-content="{{ form_errors(form.nombre)  }}"><i
                                class="fas fa-exclamation-triangle"></i></a>
                </div>
            {% endif %}
        </div>
    </div>
</div>
<div class="card-footer text-right">
    <button type="submit" class="btn btn-primary mr-2">{{ button_label|default('Añadir País') }}</button>
    <a class="btn btn-light" href="{{ path('pais_index') }}">Volver al Listado</a>
</div>
{{ form_end(form) }}